# Frontend — Personal Weather Assistant

React + Vite web UI. See the root [README](../README.md) for full setup instructions.

```bash
npm install
npm run dev   # http://localhost:5173
```

The Vite dev server proxies `/api` requests to the backend on `http://localhost:8000`.
